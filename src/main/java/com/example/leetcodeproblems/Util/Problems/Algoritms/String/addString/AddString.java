package com.example.leetcodeproblems.Util.Problems.Algoritms.String.addString;

public class AddString {
    public String addStrings(String num1, String num2) {
        int sum = 0;
        int length = Integer.parseInt(num1.length() > num2.length() ? num1 : num2);
        for (int i = 0; i < length; i++) {

        }
        return "s";
    }
}
